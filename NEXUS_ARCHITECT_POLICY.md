# Nexus Architect — سياسة تشغيل وحدود صلاحيات

## الغرض

Nexus Architect هو **مراقب صحة دوري محدود**. يعمل كل 15 دقيقة بعد تفعيل المهمة الدورية، ويقرأ المؤشرات المخزنة في Nexus ثم يسجل درجة صحة وتوصيات قابلة للمراجعة. لا يعمل كمشرف مفتوح الصلاحيات، ولا يملك مسارًا لتعديل الكود أو النشر أو الحسابات أو البيانات الحساسة تلقائيًا.

## الإجراءات المسموحة

| الإجراء | طريقة التنفيذ | الأثر |
|---|---|---|
| قراءة مؤشرات اللاعبين والعصابات والآركيد والبطولة وسجل التدقيق | قراءة فقط | لا يغير بيانات اللعب |
| حساب درجة صحة المنصة | قواعد حتمية | يسجل نتيجة قابلة للمراجعة |
| حفظ توصيات غير تنفيذية | سجل Architect | لا ينفذ التوصية ذاتيًا |
| تشغيل فحص يدوي | لوحة Architect، حساب admin فقط | ينشئ سجل فحص جديد |
| إيقاف أو استئناف المراقب الدوري | لوحة Architect، حساب admin فقط | يغير حالة مهمة المراقبة فقط |

## الإجراءات الممنوعة

| الإجراء | السبب |
|---|---|
| نشر الكود أو إنشاء نسخة إنتاجية | يحافظ على موافقة المستخدم على كل تغيير برمجي |
| تغيير قاعدة البيانات أو حذف البيانات | يمنع آثارًا غير قابلة للعكس |
| تنفيذ مدفوعات أو تغيير الأرصدة | يحمي الاقتصاد الداخلي واللاعبين |
| ترقية المستخدمين أو تجاوز المصادقة | يحافظ على مبدأ أقل صلاحية |
| إرسال رسائل أو محتوى خارجي نيابة عن المستخدم | يمنع التواصل غير المصرح به |

## مصدر السياسة والتدقيق

تُخزن السياسة في جدول `nexus_settings` بالمفتاح `nexus.architect.policy.v1`. تسجل كل جولة في `nexus_architect_runs`، وتضاف أحداث الفحص والتشغيل اليدوي والتفعيل إلى `nexus_audit_logs`. يعرض مسار `/architect` النسخة المخزنة من السياسة إلى الحسابات التي تحمل دور `admin`.

## الجدولة المفعلة

أُنشئت مهمة Heartbeat للمشروع بالمعرف `DKu52tVFTwJdFiuX84NVSc` على المسار `/api/scheduled/nexus-architect`، وهي مفعلة بتكرار كل 15 دقيقة عند الدقائق 00 و15 و30 و45 بتوقيت UTC. استُبدلت المهمة التجريبية السابقة بمهمة جديدة بعد أن أظهرت المنصة موعدًا لاحقًا متوقفًا؛ المعالج نفسه اختُبر مسبقًا بحالة HTTP 200 وسجل درجة صحة وتوصيات غير تنفيذية فقط. يحتفظ المعالج بمفتاح تشغيل ربع-ساعي لمنع تكرار السجل عند إعادة المحاولة.

## التحقق الأول المطلوب بعد إنشاء الحساب

بعد إضافة أول حساب مالك، سَجِّل الدخول ثم تحقق من أن `/architect` يعرض سياسة «مسموح/ممنوع» كاملة، وأن الأزرار الوحيدة هي **فحص آمن الآن** و**إيقاف/تفعيل الفحص الدوري**. بعد ذلك اختبر إنشاء غرفة، إرسال رسالة، نتيجة آركيد، شراء عنصر، وتبديل وضع الصيانة؛ هذه المسارات محمية ولم تُختبر إيجابيًا لغياب حساب مستخدم فعلي أثناء البناء.

# ملاحظات الاختبار الأولي

تم فتح `/play` في المعاينة الحية والتأكد من أن شاشة الاستعداد تعرض عدد العقد، مدة الجولة، حالة المسار، ومسار الرجوع. بعد الضغط على «ابدأ الجولة» انتقلت الواجهة إلى السؤال الأول دون إعادة تحميل، وظهر المؤقت والعقدة والفئة وأربعة خيارات.

الاختبار التالي هو اختيار الإجابة الصحيحة للتأكد من توقف المؤقت، ظهور الأثر التعليمي، وانتقال الجولة إلى العقدة التالية مع حفظ التقدم.

## المرحلة الأولى: الحسابات والتقدم السحابي

في 14 أغسطس 2026، فُتحت `/play` في المعاينة بعد ترقية المشروع إلى full-stack. بدأ زر «ابدأ الجولة» الجولة بنجاح، ثم أدى اختيار الإجابة الصحيحة في السؤال الأول إلى ظهور السؤال التالي وتحديث رصيد الأثر. تم التحقق من `localStorage`؛ يحتوي المفتاح `nexus-progress-v2` الآن على `updatedAt` ورقم XP وعدد إجابات محدث، ما يتيح لآلية المزامنة اختيار أحدث نسخة زمنية. ظل اختبار المتصفح في وضع «محليًا فقط» لعدم وجود جلسة حساب في بيئة المعاينة، وهو سلوك مقصود ولا يدّعي وجود أونلاين دون تسجيل دخول.

بعد تفعيل المحرك التكيفي، أُعيد فتح `/play` وظهرت شاشة الاستعداد بإشارة «تكيفي · جاهز». أدى بدء الجولة إلى عرض فئة «الأنماط» وسؤال صالح بأربعة خيارات ومؤقت يعمل، ما يؤكد اتصال الواجهة بحزمة الجولة التكيفية بدل اختيار فهرس ثابت فقط.

## المجتمع والآركيد والاقتصاد

تمت معاينة صفحات `/community` و`/arcade` و`/shop` و`/owner` على سطح المكتب، ثم على عرض هاتف 375 بكسل. بعد مراجعة الهاتف، جرى تحويل المجتمع والآركيد والملف الشخصي والمتجر إلى أعمدة مفردة أو شبكات ثنائية مناسبة، وأصبح شريط التنقل السفلي قابلًا للتمرير أفقيًا عند ضيق العرض. لا تعرض شاشات البطولة والنتائج والعصابات بيانات وهمية؛ تظهر حالات فارغة صريحة إلى أن يسجل اللاعبون بيانات فعلية.

## Nexus Architect

تمت معاينة `/architect` و`/owner` بعد إضافة Nexus Architect. أظهرت لوحة Architect بوضوح أنها مراقب محدود: زر فحص آمن، خيار إيقاف أو تفعيل المراقبة، بطاقة جدولة كل 15 دقيقة، وسجل توصيات. كما تؤكد الواجهة نصيًا أن المراقب لا ينشر كودًا ولا يحذف بيانات ولا يجري عمليات مالية أو يغير الصلاحيات تلقائيًا. تعرض لوحة المالك رابطًا واضحًا إلى Architect وسجل الإدارة وحالة الصيانة.

بعد حفظ سياسة Architect في `nexus_settings`، تم التحقق من وجود نسخة السياسة 1 في قاعدة البيانات، ثم أعيدت معاينة لوحة `/architect` المحمية. أظهرت البطاقة قائمة «مسموح» كاملة (قراءة المؤشرات، تقييم الصحة، فحص موثق، وإيقاف أو استئناف المراقبة) وقائمة «ممنوع» كاملة (النشر، حذف أو تعديل البيانات، الأرصدة، الأدوار، والمراسلة بالنيابة عن المستخدم). لا تعرض اللوحة سوى زري «فحص آمن الآن» و«إيقاف/تفعيل الفحص الدوري».

تم إنشاء مهمة Heartbeat للمراقب بالمعرف `4E5TTYTQnyvbzvvRQyMU9Z` وربطها بمعالج `/api/scheduled/nexus-architect`. نجحت عمليات تشغيل فعلية بـ HTTP 200، وسجلت درجة صحة 65 وتوصيات غير تنفيذية فقط. أُعيدت الجدولة النهائية إلى الدقائق 00 و15 و30 و45 بتوقيت UTC. يلزم مراقبة أول دورة لاحقة بعد الإرجاع للتأكد من توقيت المنصة، بينما تم التحقق من سلامة المعالج وسجل التنفيذ وهوية مهمة cron بالفعل.

أضيف اختبار ثابت لواجهة `ArchitectConsole` يتحقق من أن استدعاءات tRPC الخاصة بها محصورة في `overview` و`runNow` و`setEnabled`، ولا تستدعي إجراءات الصيانة أو الاقتصاد أو المجتمع. لا يحل هذا الاختبار محل فتح اللوحة بحساب admin فعلي؛ يظل ذلك مطلوبًا بعد إنشاء أول حساب.

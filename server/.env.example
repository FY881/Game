# Development only. Never commit real production secrets.
PORT=8080
HOST=0.0.0.0
CORS_ORIGIN=http://localhost:3000
JWT_ISSUER=mamalik-alnard-server
JWT_AUDIENCE=mamalik-alnard-android
JWT_SECRET=replace-with-a-32-byte-or-longer-random-secret
ACCESS_TOKEN_TTL_SECONDS=900
REFRESH_TOKEN_TTL_SECONDS=2592000
# Set this only after creating an Android OAuth client in Google Cloud Console.
GOOGLE_ANDROID_CLIENT_ID=
